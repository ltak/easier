# Easier iOS

SwiftUI app with modular packages.

## Structure

- `EasierApp`: app entry and feature screens
- `EasierModules`: Swift package modules for domain models, client code, and design system

## Current slice

The first feature slice is `Food Logging + Meal Editor`.
